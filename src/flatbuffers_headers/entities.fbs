include "user_interface.fbs";
namespace steamrot;


table EntityData{
  index: uint32;
    c_user_interface: UserInterface;
  }

// wrapper table to allow for metadata in the future
table EntityCollection {
  entities: [EntityData];
}

