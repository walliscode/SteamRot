// general types to use in other schemas

table Vector2f{
    x: float;
    y: float;
  }


struct Color {
    r:uint8;
    g:uint8;
    b:uint8;
    a:uint8;
  }


table Vertex {
  position: Vector2f (required);
  color: Color (required);
}


table Triangle {
vertices: [Vertex] (required);
  }
