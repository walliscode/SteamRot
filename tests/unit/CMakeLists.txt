# Unit tests directory
# Unit tests test individual classes/functions in isolation with mocked dependencies
# Run with: ctest -L unit

# This directory is reserved for future unit tests
# Existing unit tests remain in their current locations until migration
